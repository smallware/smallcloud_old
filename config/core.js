var path = require('path');

module.exports = {
  modelsPath: path.resolve(__dirname, '../core/models'),
  secret:     'El este del ese',
  host:       'localhost',
  port:       3000,
  protocol:   'http',
  version:    '0.1.0',
  silent:     false
};